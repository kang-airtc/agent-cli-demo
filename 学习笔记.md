## CLI
Command Line Interface
Dos AWS CLI GitHub CLI OpenCode CLI Claude Code CLI 
Agent --> Cli --> Tool

打开文档 OpenDoc(name)

## 安装方式
npm 
curl

### Curl安装脚本流程
基本流程：
1. 编译各平台的二进制文件（Linux/macOS/Windows）
2. 上传到 GitHub Releases
3. 写 install.sh 脚本，让它：
   - 检测操作系统和架构（amd64/arm64）
   - 从 GitHub Releases 下载对应的二进制文件
   - 解压并移动到 /usr/local/bin 或 ~/.local/bin
   - 设置执行权限
示例脚本逻辑：
#!/bin/bash
OS=$(uname -s | tr '[:upper:]' '[:lower:]')
ARCH=$(uname -m)
URL="https://github.com/user/repo/releases/latest/download/myapp-${OS}-${ARCH}.tar.gz"
curl -L $URL | tar xz && mv myapp /usr/local/bin/
常见做法：
- 把脚本放 GitHub 仓库根目录
- 通过 raw.githubusercontent.com 提供下载：
    curl -sSL https://raw.githubusercontent.com/user/repo/main/install.sh | bash



### npm 发布 CLI 的流程：
1. 创建项目
mkdir my-cli && cd my-cli
npm init -y
2. 配置 package.json
{
  "name": "my-cli",
  "version": "1.0.0",
  "bin": {
    "my-cli": "./bin/cli.js"
  },
  "preferGlobal": true
}
3. 编写入口文件 (bin/cli.js)
#!/usr/bin/env node
console.log('Hello from my-cli!');
4. 发布
npm login
npm publish



## my-agent-node-cli
npm run dev -- hello --name Alice
npm run build
npm install -g .
my-agent hello
npm uninstall -g my-agent-node-cli


## 发布npm
发布到 npm 的步骤：
1. 注册账号
- 访问 https://www.npmjs.com/signup
- 验证邮箱
2. 本地登录
npm login
# 输入用户名、密码、邮箱、一次性验证码（2FA）
3. 检查包名是否可用
npm search my-agent-node-cli
# 或直接在 npm 网站搜索
4. 准备发布（关键配置）
检查 package.json：
{
  "name": "my-agent-node-cli",     // 必须唯一
  "version": "1.0.0",              // 每次发布要递增
  "main": "dist/cli.js",           // 入口文件
  "files": ["dist", "README.md"],  // 只包含这些文件（推荐）
  "bin": {
    "my-agent": "./dist/cli.js"
  }
}

创建 .npmignore（可选，排除不需要的文件 不建议用了）：
src/
node_modules/
tsconfig.json
.gitignore

5. 构建并测试
npm run build
node dist/cli.js --help    # 确保能正常运行

6. 发布！
npm publish
7. 验证发布
npm view my-agent-node-cli    # 查看包信息
npm install -g my-agent-node-cli   # 测试安装
更新版本：
# 修改代码后，更新版本号（三选一）
npm version patch   # 1.0.0 → 1.0.1（bug修复）
npm version minor   # 1.0.0 → 1.1.0（新功能）
npm version major   # 1.0.0 → 2.0.0（不兼容变更）
# 然后重新发布
npm publish
常见问题：
- 包名已被占用 → 换一个名字
- 版本已存在 → 先 npm version 更新版本号
- 2FA 问题 → 需要邮箱或认证器 App 的验证码
发布完成后，任何人都可以通过 npm install -g my-agent-node-cli 安装你的 CLI 了！


## 发布到npmjs总结
1. 注册与认证
- 注册：https://www.npmjs.com/signup
- 免费账户必须启用 2FA
2. 构建与发布
npm run build
npm publish 打开浏览器点击验证就可以
3. 用户安装
npm install -g my-agent-node-cli
my-agent --help

## my-agent-node-cli 更新
npm update -g my-agent-node-cli


## my-agent-python-cli

### 安装
```bash
cd my-agent-python-cli
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install -e .
```

### 运行
```bash
my-agent-py --help
# 或直接
python src/cli.py <command>
```

### 构建.whl包
```bash
pip install build
python -m build
# 产物在 dist/ 目录
```

### 工作原理
`setup.py` 的 `entry_points` 在 `venv/bin/` 生成可执行脚本：
```python
entry_points={
    "console_scripts": ["my-agent-py=src.cli:cli"],
},
```
`pip install -e .` = editable install，代码改动即时生效

### 发布到 GitHub
1. 构建
```bash
pip install build
python -m build
# 产物: dist/*.whl
```
2. 推送代码并打标签
```bash
git push origin main
git tag v1.0.0
git push origin v1.0.0
```
3. 创建 Release
- GitHub 仓库 → Releases → New release
- 选择标签 v1.0.0
- 上传 dist/*.whl
4. 安装
```bash
pip install https://github.com/user/repo/releases/download/v1.0.0/*.whl
```


### install.sh 修改、发布、安装、验证流程

1. 修改 install.sh
2. 推送到 GitHub
```bash
cd my-agent-python-cli
git add install.sh
git commit -m "fix: install.sh macOS compatibility"
git push origin main
```
3. 安装
```bash
curl -fsSL https://raw.githubusercontent.com/xwzai/my-agent-python-cli/main/install.sh | bash
```
4. 使 PATH 生效（curl | bash 是子进程，无法自动修改当前终端的环境变量）
```bash
source ~/.zshrc
```
5. 验证
```bash
my-agent-py status
```